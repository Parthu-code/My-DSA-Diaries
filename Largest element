class Solution {
  public:
    int largest(vector<int> &arr) {
        int lar=arr[0];
        for(int i=0;i<arr.size();i++){
            if(arr[i]>lar) lar=arr[i];
        }
        return lar;
        
    }
};

